# 反向策略与禁止操作判定

## 1. 文档定位

本文件用于记录不允许出现、需要避免或需要单独识别的招聘操作方式。

这里维护的是“反向策略”：某些行为表面上会增加操作量或事件数，但会破坏候选人曝光、主动打招呼、聊天转化的真实判断，或者让后续分析误判招聘动作质量。

记录原则：

- 先描述不允许或需要避免的操作方式。
- 再写清它为什么会干扰策略判断。
- 最后给出可复用的日志判定规则和案例证据。
- 没有充分数据验证的规则标为“待验证”，不要写成最终结论。

## 2. 条目模板

```md
### 反向规则：ANTI-YYYYMMDD-001

- 状态：待验证 / 已观察 / 已确认 / 已废弃
- 记录时间：
- 规则名称：
- 不允许或需要避免的操作：
- 为什么有问题：
- 主要影响方向：候选人曝光 / 主动打招呼 / 聊天转化 / 数据复盘
- 分析步骤：
- 判定规则：
- 案例证据：
- 处理建议：
- 后续验证：
```

## 3. 反向规则：详情容器内连续切换候选人

- 状态：已确认
- 规则名称：详情容器内连续切换候选人
- 不允许或需要避免的操作：在列表页没有新增候选人卡片曝光、详情抽屉或详情 iframe 没有关闭的情况下，在详情容器里连续点击“下一个”或类似入口，导致连续产生大量 `candidate_detail.opened`。
- 为什么有问题：这类操作不是从候选人列表逐个筛选进入详情，会让 `candidate_detail.opened / candidate_list.card_exposed` 比例异常偏高，破坏“列表曝光 -> 详情打开”漏斗的可解释性，也容易被误判为高质量浏览。
- 主要影响方向：候选人曝光、数据复盘

### 3.1 判定说明

“详情容器内连续切换候选人”不是“重复打开同一候选人”。

它指的是：

- 列表页没有新增 `candidate_list.card_exposed`。
- 详情抽屉或详情 iframe 没有关闭。
- 操作员在详情里点了“下一个”或类似入口。
- 日志连续产生多条 `candidate_detail.opened`。

### 3.2 分析步骤

先按操作员和日期查询 raw 事件：

- `candidate_detail.opened`
- `candidate_detail.closed`
- `candidate_list.card_exposed`
- 必要时补看 `page_session.page_changed`
- 必要时补看 `candidate_filter.applied`

再依次检查：

- 看比例异常：`detail_opened / card_exposed` 明显偏高，例如超过 `3x`。
- 看分钟分布：多个分钟里 `detail_opened > 0` 但 `card_exposed = 0`。
- 看连续趋势：如果连续几分钟都是详情增长、卡片不增长，很像详情内切换，而不是从列表逐个点击。
- 看 open/close：`opened` 明显多于 `closed`。
- 看未关闭切换：如果一个详情没有 close，就出现下一条 opened，说明详情容器没关闭但候选人切了。
- 看来源字段：`payload.source` 如果大量是 `poll`，不是 `click`，更像探针持续识别详情容器变化。
- 看识别方式：`detectedBy` 如果集中在 `c_resume_canvas_matched_card` 或 `c_resume_recent_card`，说明主要来自详情 iframe/canvas 识别。
- 看详情路径：`detailUrl` path 如果集中在 `/web/frame/c-resume/`，说明事件集中在详情 iframe。
- 看 session/tab：如果大量事件都在同一个 `page_session`、同一个 tab、同一个详情 path 内发生，且没有明显页面切换，基本就是详情容器内连续操作。

### 3.3 判定规则

满足以下多数条件时，可以标记为“疑似详情容器内连续切换候选人”：

- `detail_opened / card_exposed >= 3`
- 连续分钟出现 `detail_opened > 0` 且 `card_exposed = 0`
- `opened > closed`，且存在 open 之后未 close 又出现下一条 open
- `source = poll` 占比很高
- `detectedBy` 主要是 `c_resume_canvas_matched_card`
- 事件集中在同一个 session、tab、detail iframe
- 详情 URL path 集中在 `/web/frame/c-resume/`

### 3.4 本次案例证据

- `candidate_detail.opened = 372`
- `candidate_list.card_exposed = 63`
- `candidate_detail.closed = 275`
- `detail_opened / card_exposed = 372 / 63`，约 `5.9x`
- `source = poll` 占比 `100%`
- `detectedBy = c_resume_canvas_matched_card` 为 `324 / 372`
- 全部集中在同一个 page session
- 有大量详情增长分钟没有对应卡片增长

### 3.5 处理建议

- 这类行为应从普通“列表曝光 -> 详情打开”漏斗中单独标记，避免把它当成正常逐个候选人筛选。
- 复盘时单独查看它是否属于操作员在详情内快速翻看候选人。
- 如果后续多次出现，应考虑建立专门指标，例如“疑似详情内连续切换次数”或“无新增卡片曝光的详情打开数”。
- 当前规则先作为策略库反向判定，不直接写入插件端事实事件。

## 4. 反向规则：自然流量老岗位连续满额主动招呼

- 状态：待验证
- 记录时间：2026-05-25
- 规则名称：自然流量老岗位连续满额主动招呼
- 不允许或需要避免的操作：对已经有稳定自然 `看过我` 的老岗位，连续多天把主动查看和主动打招呼拉到满额，尤其是在聊天打开、候选人新招呼、微信/电话交换没有同步增长时，仍继续把同一岗位当作冲量池。
- 为什么有问题：这类操作表面上提高了账号活跃度和主动招呼量，但如果候选人端反馈弱，可能让平台把该岗位近期样本判断为匹配效果差，或短期打穿高意向人群池，导致自然 `看过我`、牛人新招呼和候选人主动沟通掉档。该规则目前只能作为待验证假设，不能写成 BOSS 已确认惩罚机制。
- 主要影响方向：候选人曝光、主动打招呼、聊天转化、数据复盘

### 4.1 分析步骤

先按账号、岗位和日期对齐两类数据：

- BOSS 官方月报/日报：
  - `我看过`
  - `看过我`
  - `我打招呼`
  - `牛人新招呼`
  - `我沟通`
  - `交换电话`
  - `交换微信`
- 插件/CLS 聚合事实：
  - `candidate_list.card_exposed`
  - `candidate_detail.opened`
  - `candidate_greeting.succeeded`
  - `candidate_chat.opened`
  - `candidate_chat.snapshot_captured`
  - `candidate_chat.wechat_captured`

再依次检查：

- 看岗位是否已有稳定自然流量：低主动量日期仍有较稳定 `看过我`。
- 看主动动作是否突然拉满：`我看过`、`我打招呼` 连续显著高于该岗位日常基线。
- 看后段反馈是否同步变强：`牛人新招呼`、聊天打开、微信/电话交换是否跟随增长。
- 看自然流量是否随后掉档：`看过我` 是否在 24-72 小时内降到原基线的低档。
- 看恢复情况：主动量下降后，`看过我` 是否在 24-72 小时内恢复。
- 排除混杂变量：同一窗口内是否改了标题、薪资、城市、岗位状态、投放权益或招聘账号。

### 4.2 判定规则

满足以下多数条件时，可以标记为“疑似连续满额主动招呼压低自然看过我”：

- 岗位在低主动量日期仍有稳定 `看过我`。
- 同一老岗位连续 2 天以上主动 `我打招呼` 接近满额，且 `我看过` 明显高于历史日常水平。
- 同期 `牛人新招呼`、聊天打开、微信/电话交换没有同步增长，甚至下降。
- 后续 1-3 天 `看过我` 明显低于近 7 日或近 14 日基线。
- 主动量下降后，`看过我` 又恢复到原来的自然流量区间。
- 同一窗口内没有重大岗位内容、权益或城市变更。

### 4.3 案例证据

2026-05-25 对 `zhouxinyu/谢女士` 的 2026-05 月报和 CLS 聚合做过一次分析，详见：

- `zhouxinyu-exposure-drop-analysis-2026-05-25.md`

老黑板岗位 `【8000+】居家黑板主播（时薪40+可兼职）` 的官方数据：

- 2026-05-17：`我看过=119`，`我打招呼=15`，`看过我=101`，`牛人新招呼=16`
- 2026-05-18：`我看过=216`，`我打招呼=39`，`看过我=143`，`牛人新招呼=16`
- 2026-05-19：`我看过=231`，`我打招呼=40`，`看过我=37`，`牛人新招呼=2`
- 2026-05-20：`我看过=409`，`我打招呼=40`，`看过我=8`，`牛人新招呼=0`
- 2026-05-21：`我看过=492`，`我打招呼=40`，`看过我=13`，`牛人新招呼=0`
- 2026-05-22：`我看过=94`，`我打招呼=10`，`看过我=103`，`牛人新招呼=10`
- 2026-05-23：`我看过=2`，`我打招呼=0`，`看过我=112`，`牛人新招呼=10`
- 2026-05-24：`我看过=1`，`我打招呼=0`，`看过我=97`，`牛人新招呼=14`

同一窗口内，插件侧老黑板岗位后段信号在 2026-05-20 到 2026-05-21 明显偏弱：

- 2026-05-18：`chat_opened=143`，`chat_snapshots=135`，`wechat_captured=6`
- 2026-05-20：`chat_opened=0`，`chat_snapshots=0`，`wechat_captured=0`
- 2026-05-21：`chat_opened=6`，`chat_snapshots=12`，`wechat_captured=0`

### 4.4 处理建议

- 对已经能稳定吃自然 `看过我` 的老岗位，不把它连续多天当作满额主动招呼的冲量池。
- 老岗位优先做精选候选人、及时回复和后段推进，保护自然流量和候选人端反馈。
- 如需冲主动量，优先放到新岗位、测试岗位或明确实验岗位，并单独记录实验窗口。
- 每次验证只改一个变量，避免同时改标题、薪资、城市、岗位状态或投放权益。

### 4.5 后续验证

- 验证窗口：2026-05-26 到 2026-06-01，连续 7 个完整自然日。
- 验证对象：`zhouxinyu/谢女士` 的老黑板岗位。
- 验证动作：降低老黑板岗位主动招呼强度，不连续打满；只做精选候选人和及时承接。高强度主动招呼如确需执行，放到新岗位或测试岗位。
- 观察指标：每日 `我看过`、`看过我`、`我打招呼`、`牛人新招呼`、`我沟通`、`交换微信/电话`，以及插件侧聊天打开、聊天快照和微信采集。
- 复盘口径：若低主动量下 `看过我` 保持或恢复到近 7 日自然水平，同时后段反馈不下降，标记为 `倾向支持`；若低主动量下仍持续掉档，或高主动量但后段反馈强时不掉档，则保留反例并继续观察。
