# AI 协作编码规范

本规范面向人类开发者和 AI 编码助手。目标是让任何一次接手都能快速恢复上下文、低冲突协作、稳定验证，并避免把不确定的业务判断写进代码。

## 1. 项目定位

本项目是 BOSS 直聘招聘行为日志采集 Chrome 插件。

当前优先级是稳定记录招聘过程中的事实事件，包括页面会话、职位上下文、候选人列表曝光、候选人详情、打招呼、本地队列、上传和运行监控。

插件端只负责观察、记录、缓存、上传事实。策略判断、转化分析、员工评估、话术质量判断等逻辑应放在后续分析系统中。

### analysis-system 子项目保护

`analysis-system/` 是独立后端分析系统子项目，不属于本 Chrome 插件项目的默认修改范围。

- 除非用户明确授权修改后端分析系统或明确点名 `analysis-system/`，否则任何任务都不得新增、修改、删除 `analysis-system/` 下的文件。
- 插件项目不得从 `analysis-system/` 导入代码、复制配置或依赖其运行时状态。
- `analysis-system/` 与插件之间唯一共享契约是 `docs/modules/12-log-specification.md`。
- 根目录文档只维护插件、日志采集和日志契约；分析系统设计、计划、进度和实现文档维护在 `analysis-system/docs/`。
- 如果用户明确授权进入 `analysis-system/` 工作，必须先阅读 `analysis-system/AGENTS.md`，并按子项目自己的 `analysis-system/docs/ai-worklog.md` 记录任务。

### strategy 策略库保护

`strategy/` 是独立招聘策略知识库文档区，不属于 Chrome 插件项目的默认修改范围。

- 除非用户明确要求修改策略库或明确点名 `strategy/`，否则普通插件开发、日志契约、调试和打包任务不得新增、修改、删除 `strategy/` 下的文件。
- 策略库只维护招聘经验、策略假设、反向规则、复盘口径和待验证判断，不保存插件源码、后端代码、密钥、完整聊天原文或完整候选人敏感信息。
- 插件项目不得从 `strategy/` 导入代码或依赖其运行时状态；`strategy/` 也不得从父项目复制运行代码、配置或依赖。
- 策略文档可以引用 `docs/modules/12-log-specification.md` 中的事件名称和事实字段，但不能要求插件端写入策略结论。
- 如果用户明确授权进入 `strategy/` 工作，必须先阅读 `strategy/AGENTS.md`，并默认只修改 `strategy/` 下的策略文档；如根项目工作流要求记录任务，可以只追加 `docs/ai-worklog.md`，不得借策略任务修改其他父项目文件。

## 2. AI 接手前必须先读

开始改代码前，先按顺序阅读：

1. `docs/ai-worklog.md`
2. `docs/overview-design.md`
3. 与任务相关的 `docs/modules/*.md`
4. 相关源码文件和对应测试
5. `package.json` 中的脚本

如果文档和代码不一致，以当前代码行为为准，但需要在最终说明里指出差异。只有在任务明确要求时才同步修改文档。

接手时必须先查看 `docs/ai-worklog.md` 的最新记录，判断上一轮是否中断、当前任务处于哪个阶段、哪些文件已经被改动、下一步应该做什么。

## 3. 工作流

每次任务按这个顺序推进：

1. 在 `docs/ai-worklog.md` 记录任务开始，包含目标、范围、计划修改文件和当前状态。
2. 明确任务边界：确认要改的模块、输入、输出和不改的部分。
3. 查找既有模式：优先复用当前项目的模块组织、命名、错误处理和测试方式。
4. 小步修改：一次只改与目标直接相关的文件。
5. 阶段完成时立即追加开发日志，说明完成内容、改动文件、验证状态和下一步。
6. 补充或更新测试：行为变化必须有对应测试。
7. 运行验证：至少运行与修改相关的测试；无法运行时说明原因。
8. 完成任务时更新 `docs/ai-worklog.md` 的任务状态，并在最终回复说明变更点、验证命令和残余风险。

不要在同一次任务里顺手重构无关代码、调整格式、移动文件或重命名公共接口。

## 4. 任务中断与续写日志

`docs/ai-worklog.md` 是 AI 协作的运行日志，不是最终设计文档。它用于记录“现在正在做什么、做到哪一步、下一步是什么”。

任何 AI 开始实质工作前，必须先追加一条任务开始记录。不能等任务完成后再补。

任务开始记录必须包含：

- 时间
- 执行者
- 任务目标
- 当前理解
- 计划修改文件
- 不修改范围
- 验证计划
- 当前状态

开发过程中，每完成一个可交接阶段就追加一条阶段记录。阶段包括但不限于：

- 完成代码阅读
- 完成方案选择
- 完成一组文件修改
- 完成测试补充
- 发现阻塞或风险
- 验证通过或失败

阶段记录必须写清：

- 已完成什么
- 改动了哪些文件
- 当前验证结果
- 如果此刻中断，下一位 AI 应该从哪里继续

新 session 续写时必须按顺序执行：

1. 阅读 `docs/ai-worklog.md` 最新任务块。
2. 查看 `git status --short`。
3. 查看相关未提交 diff。
4. 对照日志判断代码是否停在“计划中、实现中、待验证、验证失败、已完成”。
5. 延续已有方案，除非发现明确错误。
6. 继续前先追加一条“接手记录”。

如果日志和实际 diff 不一致，以实际 diff 为准，并在日志中记录差异。

## 5. 续写友好规则

代码和文档需要让下一个 AI 可以从中间继续：

- 模块边界要清楚，文件职责单一。
- 公共函数命名表达业务含义，不用临时缩写。
- 新增状态字段、事件类型、存储结构时，要能从名字看出用途。
- 跨文件约定必须落在共享常量、类型说明或文档中，不要只藏在调用方。
- 不要依赖隐式执行顺序，必要时把生命周期阶段写进函数名或状态名。
- 对尚未真机验证的 DOM 选择器、页面结构、字段来源，用 `TODO` 标注具体待验证内容。
- 注释只解释非显然的业务原因、浏览器限制或 BOSS 页面假设，不复述代码。

推荐在复杂模块顶部保留简短结构说明：

```js
// Responsibilities:
// - observe visible candidate cards
// - emit fact events only once per page session
// - avoid storing analysis decisions in the content script
```

## 6. JavaScript 风格

本项目使用原生 ES module。

- 使用 `import` / `export`，不要引入 CommonJS。
- 字符串使用双引号。
- 缩进使用 2 个空格。
- 语句末尾使用分号。
- 优先使用 `const`，需要重新赋值时使用 `let`。
- 函数保持短小，优先通过纯函数封装可测试逻辑。
- 浏览器 API、Chrome extension API 和 DOM 访问应隔离在边界模块中。
- 共享逻辑放在 `extension/src/shared/`，content script 逻辑放在 `extension/src/content/`，background 逻辑放在 `extension/src/background/`。
- 测试文件放在 `test/`，使用 `node:test` 和 `node:assert/strict`。

示例：

```js
export function buildEventEnvelope(event, context) {
  return {
    eventId: createId(),
    eventType: event.type,
    occurredAt: new Date().toISOString(),
    context,
    payload: event.payload
  };
}
```

## 7. 事件与日志设计

日志事件必须是独立事实，不编码分析结论。

应该记录：

- 用户看到了什么
- 用户打开了什么
- 用户点击了什么
- 页面状态发生了什么变化
- 插件运行是否异常
- 本地队列和上传是否成功

不应该在插件端记录：

- 候选人是否优质
- 招聘专员是否偷懒
- 筛选策略是否有效
- 话术质量评分
- 转化归因结论

事件字段设计原则：

- `eventType` 使用稳定、可枚举的字符串。
- 每条事件必须包含发生时间、页面上下文和来源模块。
- 候选人、职位、会话等标识要尽量稳定；无法稳定时明确标注来源和置信度。
- 不把完整敏感文本随意写入日志。涉及聊天内容、简历内容、手机号、微信号等信息时，先确认采集范围。
- 上传失败不能丢数据，应进入本地队列或记录可恢复状态。

## 8. Chrome 插件边界

content script 负责观察页面和采集事实，不负责长期存储、批量上传或复杂分析。

background service worker 负责跨页面协调、队列、上传和运行状态。

shared 模块只放无副作用或低副作用的工具逻辑，例如页面分类、ID、配置、事件类型、数据结构校验。

调试页只用于开发验证，不应成为业务链路依赖。

## 9. DOM 观察规则

BOSS 页面结构可能变化，DOM 相关代码必须保守。

- 选择器要集中管理或靠近使用点，并写明页面假设。
- 不要让单个选择器失败导致整个采集链路停止。
- MutationObserver、IntersectionObserver、定时器必须有清理逻辑。
- 对重复曝光、重复点击、重复详情打开要做幂等处理。
- 页面不可见、SPA 路由切换、返回上一页、候选人弹窗关闭等场景都要考虑。
- 真机未验证的选择器不能写成“最终确定”的命名。

## 10. 错误处理

错误处理以不中断主链路为目标。

- 可恢复错误要记录 debug state 或运行监控事件。
- 不要吞掉异常后完全无痕。
- 面向用户页面的 content script 不能因为采集异常影响 BOSS 页面操作。
- 对 `chrome.*` API 调用要处理缺权限、上下文失效、service worker 重启等情况。
- 序列化日志时要避免循环引用、DOM 节点、Error 对象直接入队。

## 11. 测试规范

所有纯逻辑优先写单元测试。

必须测试：

- 页面 URL 分类
- 事件构造和字段稳定性
- 队列入队、出队、重试、失败保留
- 幂等去重
- 异常输入

推荐命令：

```bash
npm test
```

如果新增逻辑依赖浏览器环境，应尽量把核心判断抽到可用 Node 测试覆盖的纯函数中。浏览器交互部分可以保留最薄的适配层。

真机验证约束：

- 必须保留真机浏览器验证路径。可以使用 Computer Use 操作普通 Chrome 页面，也可以使用 Codex Chrome 插件读取网页内容或辅助真机验证。
- 禁止使用 Chrome DevTools Protocol、远程调试端口、`chrome.debugger`、CDP 版 Playwright/Puppeteer，或任何会把 CDP/DevTools 调试能力附加到 BOSS 直聘页面的方式；BOSS 直聘会探测调试能力并可能强制刷新页面，导致页面状态被污染。
- 如需排查 DOM 或页面结构，优先使用非 CDP 的页面读取方式、插件自身 Debug 页、事件队列、用户贴出的事件 JSON、截图或手动记录。无法确认某个工具是否走 CDP 时，先不要用于 BOSS 页面。

## 12. 文档同步

修改模块行为时，判断是否需要更新对应设计文档：

- 新增模块：更新 `docs/overview-design.md` 和新增模块文档。
- 新增事件类型：更新相关模块文档。
- 改变模块职责：更新相关模块文档。
- 只是修复实现细节：通常不需要改文档，除非原文会误导下一位接手者。

文档应描述稳定设计和已验证事实。对假设、待真机验证内容、后续计划要明确标注。

## 13. 协作与冲突处理

多人或多 AI 同时工作时：

- 不改与任务无关的文件。
- 不重排无关代码。
- 不批量格式化整个仓库。
- 发现已有未提交改动时，先判断是否与当前任务相关；无关则不要触碰。
- 如果必须修改同一文件，尽量只编辑目标函数或目标段落。
- 最终说明中列出实际改动文件。

如果遇到不确定的业务判断，优先写成可配置、可扩展或待确认项，不要把猜测固化成默认策略。

## 14. 交接输出模板

每次完成任务时，最终回复应包含：

```text
完成内容：
- ...

验证：
- npm test

注意事项：
- ...
```

如果任务只涉及很小改动，可以压缩成一句话，但必须说明是否验证。

## 15. 禁止事项

- 不在插件端实现候选人质量评分或员工绩效结论。
- 不无依据采集敏感个人信息。
- 不把真机未验证的 DOM 假设写成稳定事实。
- 不通过大范围重构掩盖小需求。
- 不提交无关格式化。
- 不新增依赖，除非现有能力明显不足且任务需要。
- 不绕过测试或忽略失败测试。
