# Tests

Tests for the analysis system live here.

Use offline CLS event samples and Profile-derived fixtures first. Tests must not require a real Tencent Cloud account, real CLS Kafka access, Chrome, or the extension runtime.
